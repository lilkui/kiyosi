# Barrier parity plan

1. Add pinned vanilla and binary rows to the CPU manifest.
2. Reconstruct every barrier row through public constructors and engines.
3. Add explicit constructor validation cases for invalid barrier inputs.
4. Run focused barrier tests, then the complete test executable.
